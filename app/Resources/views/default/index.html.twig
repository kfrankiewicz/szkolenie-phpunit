{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {% for task in tasks %}
            <div>
                <b>{{ task.title }}</b>
            </div>
        {% endfor %}
    </div>
    
    {{ form_start(form) }}    
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}